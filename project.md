# 專科護理師甄審口試實戰演練系統

## 這是什麼

給**一位外科專科護理師**在甄審口試前 3–6 週累積實戰次數的工具。
使用者是考生本人（自己練為主），偶爾有 1–2 位資深同仁陪練扮口試委員。

**不是**考場管理系統，也不是題庫產生器。前一版 `implementation_plan.md` 假設的是
「主持人辦一場甄審」，方向錯誤，已整份作廢。

---

## 規範依據

全部數字出自《110 年度專科護理師甄審口試流程及評分方式公告》，
程式中每一處都在註解裡附了條號。

### 時間規則分兩組（這是前一版最大的錯誤）

| 分科 | 規則 | 依據 |
| :--- | :--- | :--- |
| 內科、精神科、兒科、外科、婦產科、家庭科 | 門前閱讀 **2 分鐘（不計入）** ＋ 考間口試 **15 分鐘** | 公告一、(二)(四) |
| 麻醉科 | **15 分鐘含**考間外讀題 2 分鐘 ＋ 考間口試 13 分鐘 | 公告二、(四) |

前一版把麻醉科的 2+13=15 套用到外科，導致每一場都少練 2 分鐘。
現在依 `department` 自動套用，見 `src/lib/constants.js` 的 `TIMING_RULES`。

### 其他明文要求

- 口試開始、結束各響鈴 1 次；結束前 2 分鐘廣播提醒。（一、(四)）
- 執行身體健康評估時，**須同時說明檢查目的、項目及部位**——即「說做合一」，
  本系統最核心的檢核點。（一、(三)）
- 全程錄音錄影。（一、(五)）本系統錄音正是還原實況。
- 成績為 2 位委員評分總和之平均，計算至小數第 2 位、第 3 位無條件捨去，60 分及格。（一、(六)）
- **公告未規定任何細項配分。** 所以系統裡的配分沒有一個是公告來的，依可靠度分兩種：
  1. **紙本考題本身寫了配分** → 照抄，這是最可靠的來源。
  2. **紙本沒寫**（回憶版、社群教案、AI 自製題多屬此類）→ 套用 `CATEGORY_WEIGHTS`
     的四類比重當**起點**，見下方〈四類配分比重〉。那組數字來自使用者對現行實況的認知，
     **不是公告**，也不是模型推論出來的。

---

## 三個角色，一台是權威

```
考生端（筆電，已登入）──── 唯一權威：跑計時、錄音、寫資料庫
      │
      ├── Realtime broadcast ──→ 手機門前貼紙端（匿名，掃 QR）
      └── Realtime broadcast ←──→ 陪練考官端（匿名，掃 QR）
```

手機端與考官端**不碰資料庫**，只加入 Realtime 頻道。
因此 RLS 可以收斂成 `owner_id = auth.uid()`，不必為了讓陪練者評分而開匿名讀寫的破口。
考官推卡與評分透過 broadcast 送回考生端，由考生端寫入。

---

## 一場演練的流程

1. 選題 → 選自練/陪練 → 選完整/片段 → 建立場次（產生 8 碼房間碼）
2. 手機掃 QR 放門外，陪練者掃 QR 用自己的手機
3. 按開始 → 門前閱讀 2 分鐘（手機顯示貼紙與倒數）
4. 倒數結束 **響鈴** → 進入考間 15 分鐘，**自動開始錄音**
5. 提示卡揭露
   - 自練：Web Speech API 聽到觸發詞自動解鎖（手動解鎖為後備）
   - 陪練：考官在自己裝置上點擊推送
6. 剩 2 分鐘 → **紅色 Banner ＋ 語音廣播**
7. 結束響鈴 → 錄音轉 MP3 → Gemini 產出逐字稿與檢核 → 報告

---

## 報告刻意不給分數

自練場沒有委員，公告也沒有細項配分。任何由模型生出的分數只是把覆蓋率化妝成分數，
對「會不會過」沒有預測力，卻最容易被當真。所以自練場只給檢核與覆蓋率。

陪練場才有分數，且由真人打，照公告算式（`officialAverage`，用 `floor` 不用 `toFixed`——
四捨五入會讓 59.996 變成及格）。

### 考官端是三級評分，不是輸入數字

OSCE 檢核表本來就是「完全做到／部分做到／未做到」。要委員在 15 分鐘內心算
0 到 7 之間該給幾分，既不現實也不一致，而且自由輸入根本擋不住超出滿分的值。
所以改成三顆按鈕，部分做到取滿分一半四捨五入（滿分 7 分 → 4 分）。

項目依 rubric 分類分組，順序即考試進行順序；組首標出該組配分佔比換算成的
**參考配時**，讓委員抓得到節奏。配分為 null 的項目仍可記錄等第，但不計入總分。

**委員可以在鈴響之後才送分。** 結束後 `PracticeRunner` 並未卸載（報告掛在它底下），
Realtime 頻道仍開著，晚到的評分會即時寫回資料庫並更新已顯示的報告。
少了這一段，只要考生按下結束，之後送的分數就永遠遺失。

報告一律可開啟，不限 `status = 'analyzed'`——沒有逐字稿的場次仍可能有委員評分，
只准已分析的場次開報告等於把分數鎖在裡面拿不出來。

### 場次只能封存，不能刪

`sessions.archived` 是軟刪除，比照 `stations.archived`。**沒有硬刪的入口，這是刻意的**——
一場演練裡可能有陪練委員送的分數，硬刪就永遠拿不回來。
封存的語意是「這場不算數」：列表不列出，也不計入「反覆漏掉」排行榜
（排行榜是從已經濾掉 archived 的 state 算的，不必另外處理）。

## 推理速練與正式演練，練的是不同東西

**分段練習**（`mode: 'segment'`）是「同一套流程，時間縮短 ＋ 檢核只看一類」——
仍然要錄音、仍然要等 Gemini 轉逐字稿。它練的是**表達**。

但逐字稿只能告訴你「有沒有講出第三個診斷」，**不能告訴你「你的第三個診斷放錯了」**。
而且每輪要等分析，做不到密集。

所以另立**推理速練**（`DrillTab`）：看情境 → 打字填三格 → 對答案自評。一輪兩三分鐘，
不錄音、不呼叫任何 API。**它練的是內容。**

> **順序不能顛倒：內容還會錯的時候練口說，只會把錯的答案講得很流利。**

### 速練的三個設計決定

- **題庫直接讀 `prompts/samples/stations/*.md`**，不另抄一份進資料庫——
  第 1 節就是題目、第 7 節就是三格答案。用 `import.meta.glob` 動態載入，
  21 份各自切成獨立 chunk（24–32 KB），不進主 bundle。
- **自評，不用 AI 評分。** 密集練的瓶頸就是回饋延遲，AI 評分會把它加回來。
  而且答案有層次，「接不接近」由她判斷比模型準。
- **紀錄存 localStorage 不進 Supabase。** 純個人速練資料，不需跨裝置、不值得開一張表。
  正式演練的成績才進資料庫。

### 排行榜只統計第三格

①②答錯多半是知識不足，多讀就好；**③ 想不出來是思維習慣的問題**——
想不到「還有什麼漏掉會出事」，那是跨題目可以累積的東西。

### 一個不能省的警告

那 21 份**沒有經過人工審核**。所以速練頁每一題都顯示這一點，
而且對答案時**強制一併顯示第 9 節「模型自己不確定的地方」**——
否則她會把模型不確定的東西當標準答案背下來。

### 檢核分兩層

- **通用骨架**（`CORE_CHECKLIST`，18 項，四大分類直接對應公告一、(一)）：
  不隨題目變動，因此可以跨場次累積成「反覆漏掉」排行榜。
- **題內專屬**：由該題 rubric 動態產生，只出現在單場報告。
  片段特訓的段落也是由 rubric 的 `category` 動態產生，換題就換段。

### 明說的限制

純錄音只能驗證「她說了什麼」，無法得知她的手做了什麼，
所以「做了卻沒說」抓不到。報告首頁就寫明這一點，不假裝系統辦得到。

---

## 技術棧與檔案位置

| 模組 | 位置 |
| :--- | :--- |
| 領域常數（時間規則、通用檢核骨架） | `src/lib/constants.js` |
| Supabase client 與 Edge Function 呼叫 | `src/lib/supabase.js` |
| Realtime 頻道與房間碼 | `src/lib/realtime.js` |
| 錄音、降取樣、MP3 編碼 | `src/lib/audio.js` |
| 錄音本機倉庫（IndexedDB，留最近 20 場） | `src/lib/idb.js` |
| 關鍵詞語音解鎖 | `src/lib/speech.js` |
| 鈴聲與廣播（Web Audio 合成） | `src/lib/sound.js` |
| 公告算式與時間格式 | `src/lib/format.js` |
| 演練主控（計時、錄音、提示卡、廣播） | `src/components/PracticeRunner.jsx` |
| 手機門前貼紙端 | `src/components/DoorView.jsx` |
| 陪練考官端 | `src/components/CoachView.jsx` |
| 報告 | `src/components/SessionReport.jsx` |
| 紀錄與反覆漏掉排行榜 | `src/components/SessionsTab.jsx` |
| 題庫編輯器 / OCR 匯入 / 貼上 JSON 匯入 | `src/components/StationEditor.jsx`、`StationsTab.jsx` |
| 出題頁（兩段 prompt ＋ 流程） | `src/components/BuildTab.jsx` |
| 推理速練（三格快答） | `src/components/DrillTab.jsx`、`src/lib/drillDeck.js` |
| 逐字稿與檢核（Gemini） | `supabase/functions/analyze-session/` |
| 紙本考題 OCR | `supabase/functions/ocr-station/` |
| 評分表重組草稿 | `supabase/functions/derive-rubric/` |

前端 React 19 + Vite，部署在 Vercel；後端 Supabase（`rideujvpbyatlggmeubr`，ap-northeast-1）。

---

## 資料流的三個邊界

1. **Gemini 金鑰**只存在於 Supabase Edge Functions 的 secrets，不進前端 bundle。
2. **錄音檔本體**留在瀏覽器 IndexedDB。送去分析的 MP3 由 Google 端在 48 小時內自動刪除，
   Edge Function 用完也會主動刪。雲端只存逐字稿與檢核結果（純文字）。
3. **陪練者**是匿名的，只能對「當下這一場」推卡與送分，看不到題庫與歷史紀錄。

---

## 題庫來源

口試題官方不公開（衛福部只公布筆試試題）。網路上流通的都是考生回憶版。
實際來源依可靠度：

1. 華杏《專科護理師口試應考策略──OSCE 技能模擬試題》— 拍照 OCR 匯入
2. 台灣專科護理師學會「OSCE 暨臨床能力訓練工作坊」Part I 線上 / Part II 實作（往年 10 月初）
3. FB「專科護理師考試練功坊」、Dcard 護理版 — 回憶版心得
4. 衛福部護理及健康照護司 — 僅筆試試題與標準答案

OCR 產出一律標「未審核」，必須人工逐項確認後才能轉「已審核」。
臨床正確性由人背書，不由模型背書。

### 匯入的教案通常沒有評分表

回憶版、社群教學案例、貼文截圖幾乎都只有情境與診斷，不附評分表，
而 OCR 依設計不會替它發明一份。但沒有評分表就無法用片段特訓（段落來自 rubric 分類）、
陪練也無法評分、報告只剩通用骨架那一層。

編輯器因此提供兩條補救，都需要人主動按：

- **套用公告四大分類骨架**（`RUBRIC_SKELETON`）：純文字，措辭直接取自公告一、(一)(三)，
  零 AI 推論，配分留空。目的只是讓片段特訓切得出段落。
- **由本題內容產生草稿**（`derive-rubric`）：把這一題已經抄進來的問診內容、身評發現與
  診斷重點**重組**成評分項，每條附上依據的原文供核對。允許重組，禁止發明——
  教案沒提到的檢查、診斷、處置一律不得寫入，配分一律 null。

### 第三條路：由情境自製題目（`prompts/`）

手邊沒有紙本題、只有一段臨床情境時，用 `prompts/` 下的兩份 prompt 走兩段式：

| 檔案 | 做什麼 | 允許發明？ |
| :--- | :--- | :--- |
| `A_differential-reasoning.md` | 情境 → 鑑別診斷推理文件（焦點式 PE、預期 sign、檢查順序、前三診斷） | **是，唯一一步** |
| `B_reasoning-to-station.md` | 審核過的文件 → station JSON（門前貼紙、提示卡、評分表） | 否，規則同 `derive-rubric` |

拆成兩段是為了讓**發明只發生一次**：A 產出的臨床事實由人審一次，
B 之後每一條評分項都能回溯到 A 的原文。混在一份 prompt 裡就分不出哪條有依據。

**怎麼跑**：App 的「出題」分頁（`BuildTab.jsx`）。兩段 prompt 直接顯示在頁面上，
貼入情境後按「複製整段 Prompt」拿去餵外部 AI，旁邊列出六步驟流程並標出**兩道人工關卡**。

> ⚠️ **prompt 內容用 Vite 的 `?raw` 讀自 `prompts/*.md`，頁面不另存一份。**
> 剛因為 `CATEGORY_WEIGHTS` 手抄三處踩過坑——prompt 一旦有兩份，改了一份忘了另一份，
> 畫面就會教錯東西。切法是 `raw.split(/^---$/m)[1]`，兩份 md 的結構都固定是
> 說明 →`---`→ 本體 →`---`→ 使用後必做（表格裡的 `| :--- |` 不是獨立成行，不會誤切）。

**怎麼進系統**：`StationsTab` 的「貼上 JSON 匯入」按鈕。
B 的輸出形狀當初就是照 `draftToStation()` 設計的，所以貼進去直接轉得動；
模型愛加的 ` ``` ` 圍欄會被容忍掉。`source` 預設 `ai`，
規矩與 OCR 相同——**一律進編輯器逐項確認，不直接寫進題庫**。
目前 A 與 B 都在外部 AI 跑（不佔 Gemini 額度）；若之後要做成 Edge Function 再說。

A 的三個設計重點：

1. **檢查依「床邊 → 非侵入 → 有輻射 → 侵入」排序**，且要寫出「本題我不排 ___，因為 ___」。
   實跑 15 題後發現這條規則的價值不在「少開 CT」——它在不同題目會給出相反的答案
   （膽道題不排 CT、腸阻塞題非排不可、頭部外傷題直接開），但被迫每次寫出理由。
2. **前三診斷的第三格放的是漏掉會出事的 can't-miss**，不是「第三可能」，且只列最低限度的排除方法。
3. **硬性時間預算**：身評表最多 8 列、標「必做」者不超過 6 項，並在第 10 節自檢
   「只剩 5 分鐘要放掉哪三項」。這條是實跑後補的——模型預設會產出 9–12 項身評，
   在 15 分鐘的考試裡是教錯節奏。

### Prompt A 必須守住的三件考試形式（實跑後才校正過來的）

素材來自醫師國考 OSCE，但**產出一律要回歸專科護理師甄審的形式**。三處最容易套錯：

1. **委員不提問。** 考生全程單向陳述，沒有問答。
   最初版把第 8 節寫成「委員最可能追問的五題」，那是醫師 OSCE 的形式。
2. **順序是「先病人、後委員」。** 身體評估做完 → 對病人收尾並告知會去通知醫師（1 分鐘）
   → 才向委員報告鑑別診斷（3 分鐘）。這反映專師的真實流程：**先替醫師評估病人，
   評估完在權限內處置、向病人交代，然後去報告醫師。**
3. **權責邊界。** 專師不開醫囑、不決定治療方針。
   對病人**不可預告會建議醫師排什麼檢查**——檢查的建議只出現在向委員的報告裡。
   對病人只說「**我會去向您的主治醫師報告，由醫師決定**」。

`samples/stations/` 下 20 份實跑輸出中，**A-30／41／56／34／62 這 5 份為現行格式**；
其餘 15 份為舊版（無必做欄、無第 10 節、第 8 節形式錯誤），
**臨床內容仍可用，形式一律以 `A_differential-reasoning.md` 現行版本為準**。
分流結果見 `samples/sp63-triage.md`。

這樣產出的題目 `source` 標 `ai`，與紙本 OCR 的 `ocr` 分開——紙本題的臨床內容有書背書，
這一份沒有。

### 四類配分比重

**臨床推理與決策 45／身體評估（說做合一）25／病史詢問 15／溝通 15**，見
`src/lib/constants.js` 的 `CATEGORY_WEIGHTS`。

這組數字**不是公告規定的**——公告完全沒提細項配分。它來自使用者對現行甄審實況的認知。
之所以要寫死，是因為考官端的「參考配時」由配分佔比換算：比重錯了就會叫委員
在最吃重的那一段少留時間，等於教錯節奏。

分類總分決定後，同一類內部誰輕誰重屬於判斷，不是原始資料裡的事實，
所以自動產生的評分表一律**平均分配**，留給人在編輯器裡調。
改比重時 `CATEGORY_WEIGHTS`、`prompts/B_reasoning-to-station.md`、
`supabase/functions/derive-rubric/` 三處要一起改。
